<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.order.dao.OrderDetailDao">

    <select id="findByOrderId" parameterType="string" resultType="io.renren.modules.order.entity.OrderDetailEntity">
        SELECT * FROM order_detail WHERE order_id = #{orderID}
    </select>

</mapper>